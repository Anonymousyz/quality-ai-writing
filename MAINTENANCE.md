# 维护说明

## 修法节奏

每攒约 **10 条案例**或每**季度**审阅一次标准。

## 修法检查清单

1. **最密维度** — 哪些维度案例积累最多？是否需要加强标准表述？
2. **未接住病征** — 哪些 AI 初稿病征尚未被标准覆盖？
3. **从未被引用的原则** — 哪条原则从未被判例引用？考虑降级或删除。

修订后更新 `STANDARD.md` 版本号，并在 `CHANGELOG.md` 中注明依据的公开案例 ID。

**覆盖抽查：** `python scripts/case_coverage.py` — 八维应各有 ≥1 条判例标签；某维长期为 0 则优先补判例或考虑降级。

## 规则准入（进入当前标准的条件）

一条规则要写进 `STANDARD.md` / 默认流程，须同时满足：

1. **对应反复出现的真实问题** — 不是偶然一次的文风偏好  
2. **适用条件与例外清楚** — 写明在什么文体/场景有效，何时不适用  
3. **在不同文本中产生可观察改善** — 读者理解、判断或行动变清楚，而不只是「看起来更规范」  
4. **收益大于执行负担** — 不增加无效检查

**边界：**

- 单次成功只能形成**待验证启发**，不能单独升格为默认规则  
- 作者声望、跨文化「都这么说」、内部评分，都不能单独证明规则有效  
- 真实文本与目标读者反馈，优先于体系自我证明  
- 重复无效的规则应删除；偶尔有用的留在 `notes/` 或 `traditions/`，不进入默认八维

## 入库与脱敏

- 进入 `cases/` 的材料必须**虚构化**；不含真实客户、雇主、项目数据
- `inbox/`、改前改后原文、修法草稿**不进入版本库**（见 `.gitignore`）
- CHANGELOG **只写结论**与公开案例 ID，不写可识别的原文档名

**入库决策：** 删掉人名、项目名、内部数字后，仍保留可示范的取舍 → 可改写成公开判例；删完只剩空壳，或仍能被认出真实来源 → **禁止入库**。

## 本仓库收录范围

| 材料 | 是否收录 |
|---|---|
| 标准维度、评审问句、一票否决 | 是 |
| 过程短注、中英传统短摘 | 是（短摘、注出处） |
| 改稿判例 | 仅虚构/脱敏后 |
| `inbox/` 与未脱敏原文 | 否 |
| CHANGELOG | 只记结论与公开案例 ID |
